services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: details_share_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
